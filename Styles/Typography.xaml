<?xml version="1.0" encoding="utf-8"?>
<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!--
        Project-specific typography. The Fluent type ramp (TitleLargeTextBlockStyle,
        SubtitleTextBlockStyle, BodyTextBlockStyle, CaptionTextBlockStyle) ships with
        XamlControlsResources. Only derive styles here when a variant is not already
        in Fluent.
    -->

    <Style x:Key="AppTitleTextBlockStyle" TargetType="TextBlock" BasedOn="{StaticResource TitleTextBlockStyle}">
        <Setter Property="FontWeight" Value="SemiBold" />
    </Style>

    <Style x:Key="AppStatusPillTextBlockStyle" TargetType="TextBlock" BasedOn="{StaticResource CaptionTextBlockStyle}">
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="CharacterSpacing" Value="32" />
    </Style>
</ResourceDictionary>
