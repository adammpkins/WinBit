<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="WinBit.Views.Settings.BitTorrentPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="Transparent">

    <ScrollViewer HorizontalScrollMode="Disabled">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="32" />
                <ColumnDefinition Width="*" MaxWidth="880" />
                <ColumnDefinition Width="32" />
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="1" Spacing="12" Margin="0,24,0,48">
            <TextBlock Text="BitTorrent" Style="{ThemeResource TitleLargeTextBlockStyle}" />
            <TextBlock Text="Peer-discovery protocols and transport encryption."
                       Style="{ThemeResource BodyTextBlockStyle}"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                       TextWrapping="Wrap"
                       Margin="0,0,0,12" />

            <controls:SettingsCard Header="DHT"
                                   Description="Distributed Hash Table (BEP 5) for trackerless peer discovery.">
                <controls:SettingsCard.HeaderIcon>
                    <FontIcon Glyph="&#xE968;" />
                </controls:SettingsCard.HeaderIcon>
                <ToggleSwitch x:Name="DhtToggle" IsOn="True" Toggled="OnDhtToggled" />
            </controls:SettingsCard>

            <controls:SettingsCard Header="Peer Exchange (PEX)"
                                   Description="Swap peer lists with connected clients (BEP 11).">
                <ToggleSwitch x:Name="PexToggle" IsOn="True" Toggled="OnPexToggled" />
            </controls:SettingsCard>

            <controls:SettingsCard Header="Local Peer Discovery (LSD)"
                                   Description="Multicast peer discovery on the local network (BEP 14).">
                <ToggleSwitch x:Name="LsdToggle" IsOn="True" Toggled="OnLsdToggled" />
            </controls:SettingsCard>

            <controls:SettingsCard Header="Encryption"
                                   Description="Message Stream Encryption handshake preference.">
                <controls:SettingsCard.HeaderIcon>
                    <FontIcon Glyph="&#xE72E;" />
                </controls:SettingsCard.HeaderIcon>
                <ComboBox x:Name="EncryptionCombo"
                          SelectedIndex="0"
                          MinWidth="160"
                          SelectionChanged="OnEncryptionChanged">
                    <ComboBoxItem Content="Prefer" Tag="Prefer" />
                    <ComboBoxItem Content="Require" Tag="Require" />
                    <ComboBoxItem Content="Disable" Tag="Disable" />
                </ComboBox>
            </controls:SettingsCard>

            <controls:SettingsCard Header="Share limits"
                                   Description="Global ratio / seeding-time caps and the action to take on trigger.">
                <controls:SettingsCard.HeaderIcon>
                    <FontIcon Glyph="&#xE8C8;" />
                </controls:SettingsCard.HeaderIcon>
                <Button Content="Configure…"
                        Click="OnConfigureShareLimitsClicked"
                        AutomationProperties.Name="Configure global share limits" />
            </controls:SettingsCard>
        </StackPanel>
        </Grid>
    </ScrollViewer>
</Page>
