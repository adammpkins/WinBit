<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="WinBit.Views.Settings.RssPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="Transparent">

    <ScrollViewer HorizontalScrollMode="Disabled">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="32" />
                <ColumnDefinition Width="*" MaxWidth="880" />
                <ColumnDefinition Width="32" />
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="1" Spacing="12" Margin="0,24,0,48">
            <TextBlock Text="RSS" Style="{ThemeResource TitleLargeTextBlockStyle}" />
            <TextBlock Text="Feed polling and auto-downloader behavior."
                       Style="{ThemeResource BodyTextBlockStyle}"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                       TextWrapping="Wrap"
                       Margin="0,0,0,12" />

            <controls:SettingsCard Header="Enable RSS feed polling"
                                   Description="Periodically refresh subscribed feeds in the background.">
                <controls:SettingsCard.HeaderIcon>
                    <FontIcon Glyph="&#xE704;" />
                </controls:SettingsCard.HeaderIcon>
                <ToggleSwitch x:Name="RssEnabledToggle" IsOn="True" />
            </controls:SettingsCard>

            <controls:SettingsCard Header="Refresh interval"
                                   Description="Minutes between scheduled feed refreshes.">
                <NumberBox x:Name="RefreshIntervalBox"
                           Value="30"
                           Minimum="1"
                           Maximum="1440"
                           SpinButtonPlacementMode="Compact"
                           MinWidth="140" />
            </controls:SettingsCard>

            <controls:SettingsCard Header="Max articles per feed"
                                   Description="Older articles beyond this limit are dropped.">
                <NumberBox x:Name="MaxArticlesBox"
                           Value="100"
                           Minimum="1"
                           Maximum="10000"
                           SpinButtonPlacementMode="Compact"
                           MinWidth="140" />
            </controls:SettingsCard>

            <controls:SettingsCard Header="Auto-downloader"
                                   Description="Evaluate rules against new articles and add matches as torrents.">
                <controls:SettingsCard.HeaderIcon>
                    <FontIcon Glyph="&#xE896;" />
                </controls:SettingsCard.HeaderIcon>
                <ToggleSwitch x:Name="AutoDownloadToggle" />
            </controls:SettingsCard>
        </StackPanel>
        </Grid>
    </ScrollViewer>
</Page>
